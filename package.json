{
  "name": "morgan-lwes",
  "description": "http request logger middleware for node.js, which emits LWES events instead of logging to disk - based on and forked from morgan",
  "version": "1.0.0",
  "author": {
    "name": "Joshua P. Mervine",
    "email": "joshua@mervine.net",
    "url": "http://mervine.net",
    "twitter": "https://twitter.com/mervinej"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/jmervine/morgan-lwes.git"
  },
  "bugs": {
    "mail": "joshua@mervine.net",
    "url": "https://github.com/jmervine/morgan-lwes/issues"
  },
  "dependencies": {
    "liblwes": "^0.3.6"
  },
  "devDependencies": {
    "istanbul": "0.2.10",
    "mocha": "~1.19.0",
    "should": "~3.3.1",
    "supertest": "~0.12.0"
  },
  "engines": {
    "node": ">= 0.8.0"
  },
  "scripts": {
    "test": "mocha --require should --reporter dot",
    "test-cov": "istanbul cover node_modules/mocha/bin/_mocha -- --require should --reporter dot",
    "test-travis": "istanbul cover node_modules/mocha/bin/_mocha --report lcovonly -- --require should --reporter spec"
  }
}
